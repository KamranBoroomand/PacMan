{
  "name": "pacman-static",
  "version": "1.0.0",
  "private": true,
  "description": "Static Pac-Man browser game with gameplay checks",
  "scripts": {
    "lint": "node scripts/lint.js",
    "test": "node --test tests/*.test.js",
    "check": "npm run lint && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
